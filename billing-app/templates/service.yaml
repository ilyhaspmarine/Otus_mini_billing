apiVersion: v1
kind: Service
metadata:
  name: {{ include "billing-app.serviceName" . | quote }}
  labels:
    app: {{ include "billing-app.appName" . | quote }}
spec:
  selector:
    app: {{ include "billing-app.appName" . | quote }}
  ports:
    - name: {{ .Values.service.portName }}
      protocol: TCP
      port: {{ int .Values.service.port }}
      targetPort: 8000
  type: {{ .Values.service.type }}
